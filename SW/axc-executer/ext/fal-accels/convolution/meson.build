project('approximate-convolution-accelerator', 'cpp',
  version : '0.1',
  default_options : ['warning_level=3'])

include_dir = include_directories('include')

add_project_arguments('-DSW_ONLY', language : 'cpp')

subdir('tests')
